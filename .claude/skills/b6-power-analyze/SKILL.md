---
name: b6-power-analyze
description: |
  B6x BLE 项目功耗分析工具，基于 BLE 参数配置计算平均功耗并给出优化建议。

  TRIGGER when: 用户要求分析功耗、功耗分析、电池续航、功耗优化、省电、低功耗优化、电流计算、电池寿命估算、BLE 功耗、蓝牙功耗、连接参数优化、广播参数优化、功耗报告。即使用户没有明确说"功耗分析"，只要涉及 B6x 项目的电池续航、电流消耗、省电策略分析，也应使用此 skill。此 skill 专注于"功耗数值计算和优化建议"，而非库选择或硬件配置验证。

  DO NOT TRIGGER when: 用户要求 BLE 库选择（用 /b6-library-recommend）、代码审查（用 /b6-code-review）、硬件配置验证（用 /b6-validate-hardware）、编译构建（用 /b6-build）。这些 skill 有不同的关注点：library-recommend 关注库和 RAM 选择，validate-hardware 关注引脚/时钟约束，而 power-analyze 关注"功耗计算和电池续航分析"。
user-invocable: true
disable-model-invocation: true
allowed-tools: Read, Grep, Glob, WebSearch, mcp__b6x-mcp-server__search_sdk
---

# B6 Power Consumption Analyzer

分析 B6x BLE 项目功耗，计算平均电流消耗，估算电池续航，并给出优化建议。

**核心定位**: 此 skill 专注于"功耗数值计算"——基于 BLE 参数配置计算平均功耗。不负责库选择（用 `/b6-library-recommend`）或硬件约束验证（用 `/b6-validate-hardware`）。

## 触发

`/b6-power-analyze <project_path> [options]`

**参数**:
- `project_path`: 项目路径，默认当前目录
- `--battery XXX`: 电池容量 (mAh)，默认 200

---

## 与其他 Skill 的区别

| 分析类型 | 使用 Skill | 关注点 |
|----------|------------|--------|
| 功耗计算/电池续航 | `/b6-power-analyze` | 电流数值、优化建议 |
| BLE 库选择 | `/b6-library-recommend` | 库类型、RAM 配置 |
| 硬件约束验证 | `/b6-validate-hardware` | 引脚冲突、时钟/DMA |
| 代码质量审查 | `/b6-code-review` | MISRA C、安全性 |

---

## B6x 芯片功耗参考数据 (3.3V)

这些数据来自芯片规格书，是功耗计算的基础：

| 工作模式 | 平均功耗 | 持续时间 | 说明 |
|----------|----------|----------|------|
| Sleep 模式 | 15 µA | 持续 | 轻度睡眠，RAM 保持 |
| PowerOff 模式 | 2 µA | 持续 | 深度睡眠，最低功耗 |
| Sleep 广播 | 4.6 mA | 5.8 ms | 从唤醒到睡眠的 active 时间 |
| Sleep 空包连接 | 3.25 mA | 4.7 ms | CI=30ms 时的 active 时间 |
| PowerOff 广播 | 4.0 mA | 8.3 ms | 从唤醒到睡眠的 active 时间 |
| PowerOff 空包连接 | 3.0 mA | 7.5 ms | CI=30ms 时的 active 时间 |

**为什么重要**: 不同睡眠模式的电流差异达 7 倍 (15µA vs 2µA)，active 时间也不同。选择正确的模式对续航影响巨大。

---

## BLE 参数说明

理解这些参数是功耗分析的基础：

### 广播参数

| 参数 | 范围 | 对功耗的影响 |
|------|------|--------------|
| Advertising Interval | 20ms ~ 10.24s | 间隔越长，平均功耗越低 |
| Advertising Type | 可连接/不可连接/定向 | 定向广播功耗是普通广播的 2-3 倍 |
| Advertising Duration | 可配置 | 超时后进入深度睡眠 |

### 连接参数

| 参数 | 范围 | 对功耗的影响 |
|------|------|--------------|
| Connection Interval (CI) | 7.5ms ~ 4s | 间隔越长，唤醒次数越少，功耗越低 |
| Slave Latency | 0 ~ 499 | 可跳过的连接事件数，越大功耗越低 |
| Supervision Timeout | 100ms ~ 32s | 影响连接稳定性，对功耗影响较小 |

**关键概念 - 有效连接间隔**:
```
Effective_CI = CI × (1 + Slave Latency)
```
例如: CI=100ms, Latency=9 → 有效间隔 = 1000ms，从机每秒只唤醒一次。

---

## 功耗计算公式

### 广播态功耗

```
I_avg = (I_active × T_active + I_sleep × T_sleep) / T_interval

其中:
- I_active: 广播事件平均电流 (4.0~4.6 mA，根据睡眠模式)
- T_active: 广播事件持续时间 (5.8~8.3 ms，根据睡眠模式)
- I_sleep: 睡眠电流 (15µA Sleep 或 2µA PowerOff)
- T_sleep: 睡眠持续时间 = T_interval - T_active
- T_interval: 广播间隔
```

**示例计算** (Sleep 模式, 广播间隔 100ms):
```
I_avg = (4600µA × 5.8ms + 15µA × 94.2ms) / 100ms
      = (26680 + 1413) / 100
      = 280.93 µA
```

### 连接态功耗

```
I_avg = (I_active × T_active + I_sleep × T_sleep) / Effective_CI

其中:
- I_active: 连接事件平均电流 (3.0~3.25 mA，根据睡眠模式)
- T_active: 连接事件持续时间 (4.7~7.5 ms，根据睡眠模式)
- I_sleep: 睡眠电流 (15µA Sleep 或 2µA PowerOff)
- T_sleep: 睡眠持续时间 = Effective_CI - T_active
- Effective_CI: 有效连接间隔 = CI × (1 + Latency)
```

**示例计算** (PowerOff 模式, CI=100ms, Latency=9):
```
Effective_CI = 100 × (1 + 9) = 1000ms
I_avg = (3000µA × 7.5ms + 2µA × 992.5ms) / 1000ms
      = (22500 + 1985) / 1000
      = 24.49 µA
```

---

## 执行步骤

### Step 1: 学习 BLE 参数知识 (可选)

**为什么**: BLE 功耗优化领域有最新研究，联网搜索可获取最佳实践。

使用 WebSearch 搜索:
- `BLE advertising interval power consumption`
- `BLE connection interval latency optimization`
- `Bluetooth low energy power calculation`

### Step 2: 读取项目配置

**为什么**: 需要从实际代码中提取 BLE 参数配置，才能进行准确计算。

查找并读取以下文件:

| 文件路径 | 提取参数 |
|----------|----------|
| `src/user_app.c` | 应用逻辑、唤醒源 |
| `src/user_cfg.c` | 用户配置 |
| `ble/app/src/app_ble.c` | BLE 初始化参数 |
| `cfg/ble_config.h` 或 `inc/cfg.h` | BLE 宏定义 |

提取关键参数:
```c
APP_ADV_INT_MIN / APP_ADV_INT_MAX   // 广播间隔 (单位: 0.625ms)
APP_CONN_INT_MIN / APP_CONN_INT_MAX // 连接间隔 (单位: 1.25ms)
APP_CONN_LATENCY                    // 从机延迟
APP_CONN_TIMEOUT                    // 超时时间 (单位: 10ms)
APP_ADV_DURATION                    // 广播超时 (单位: 10ms)
```

### Step 3: 识别睡眠模式

**为什么**: Sleep (15µA) 和 PowerOff (2µA) 电流差异 7 倍，直接影响续航计算。

检查代码中的睡眠模式配置:
- `pmu_sleep_enter()` → Sleep 模式
- `pmu_poweroff_enter()` → PowerOff 模式

### Step 4: 计算各状态功耗

**为什么**: 实际使用是各状态的组合，需要加权平均。

计算:
1. **广播态平均电流** - 根据广播间隔和睡眠模式
2. **连接态平均电流** - 根据连接参数和睡眠模式
3. **待机态电流** - 深度睡眠时的静态电流

### Step 5: 估算电池续航

**为什么**: 将电流转换为用户关心的"使用时间"。

```
续航时间 (小时) = 电池容量 (mAh) / 平均电流 (mA) × 效率系数
续航天数 = 续航时间 / 24
```

效率系数通常取 0.7-0.8 (考虑电池老化、温度等因素)

### Step 6: 优化建议

**为什么**: 给出可操作的改进建议，帮助用户延长续航。

根据应用场景推荐配置:

| 应用场景 | 连接间隔 | 从机延迟 | 广播间隔 | 预期续航 |
|----------|----------|----------|----------|----------|
| HID 键盘/鼠标 | 30-100ms | 4-10 | 100-500ms | 数月 |
| 传感器 (低频) | 100-1000ms | 0-9 | 1-10s | 数年 |
| 信标 (Beacon) | N/A | N/A | 1-10s | 数年 |
| 实时数据传输 | 7.5-30ms | 0 | 20-100ms | 数天 |
| 智能手表 | 30-100ms | 2-5 | 100-500ms | 数周 |

---

## 输出格式

```markdown
================================================================================
                        B6x BLE 功耗分析报告
================================================================================

项目: bleHid_Uart
分析时间: 2026-03-23

--------------------------------------------------------------------------------
当前配置
--------------------------------------------------------------------------------

| 参数 | 当前值 | 单位 | 建议范围 |
|------|--------|------|----------|
| 广播间隔 | 160 | ms | 20-10240 |
| 连接间隔 | 30 | ms | 7.5-4000 |
| 从机延迟 | 4 | - | 0-499 |
| 超时时间 | 5000 | ms | 100-32000 |
| 睡眠模式 | Sleep | - | Sleep/PowerOff |

--------------------------------------------------------------------------------
功耗计算
--------------------------------------------------------------------------------

广播态:
  平均电流: 281.0 µA
  计算过程: (4600 × 5.8 + 15 × 154.2) / 160 = 281.0 µA

连接态:
  平均电流: 548.7 µA
  有效连接间隔: 150 ms (30 × (1+4))
  计算过程: (3250 × 4.7 + 15 × 145.3) / 150 = 548.7 µA

待机态:
  睡眠电流: 15 µA

--------------------------------------------------------------------------------
电池续航估算
--------------------------------------------------------------------------------

假设使用场景: 8小时/天连接，16小时/天待机

| 状态 | 时间占比 | 平均电流 |
|------|----------|----------|
| 广播 | 5% | 281.0 µA |
| 连接 | 33% | 548.7 µA |
| 待机 | 62% | 15.0 µA |

综合平均电流: 197.4 µA

电池容量: 200 mAh
效率系数: 0.75
预计续航: 200 / 0.1974 × 0.75 = 760 小时 ≈ 31.7 天

--------------------------------------------------------------------------------
优化建议
--------------------------------------------------------------------------------

1. [高优先级] 增加从机延迟
   - 当前: 4
   - 建议: 9
   - 预期效果: 连接态电流降低 ~50%，续航增加 ~15 天

2. [中优先级] 切换到 PowerOff 模式
   - 当前: Sleep (15 µA)
   - 建议: PowerOff (2 µA)
   - 预期效果: 待机电流降低 87%，续航增加 ~5 天
   - 注意: 需要评估唤醒时间是否满足响应要求

3. [低优先级] 增加广播间隔
   - 当前: 160 ms
   - 建议: 500 ms
   - 预期效果: 广播态电流降低 ~60%
   - 注意: 会增加连接建立延迟

--------------------------------------------------------------------------------
注意事项
--------------------------------------------------------------------------------

- 实际功耗受环境温度、电池电压、射频干扰等因素影响
- iOS 设备对连接参数有限制: CI 建议 30-120ms，Latency ≤ 30
- Android 设备通常更宽松，但不同厂商有差异
- 定向广播功耗是普通广播的 2-3 倍

================================================================================
```

---

## 注意事项

1. **理论值 vs 实际值**: 计算结果为理论参考，实际功耗需用电流表测量
2. **平台限制**: iOS/Android 对 BLE 参数有不同限制，优化时需考虑兼容性
3. **响应速度权衡**: 低功耗通常意味着更高的延迟，需平衡用户体验
4. **温度影响**: 低温环境下电池容量会下降，需留有余量
5. **唤醒时间**: PowerOff 模式唤醒时间比 Sleep 长，需评估是否满足实时性要求

---

## 与其他 Skill 协作

| 顺序 | Skill | 用途 |
|------|-------|------|
| 1 | `/b6-library-recommend` | 选择合适的 BLE 库 |
| 2 | `/b6-create-project` | 创建项目 |
| 3 | `/b6-validate-hardware` | 验证硬件配置 |
| 4 | `/b6-power-analyze` | 分析功耗 (当前) |
| 5 | `/b6-build` | 编译验证 |

对于电池供电产品，建议在设计阶段就进行功耗分析。
